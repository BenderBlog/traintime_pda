name: Build for Android

on:
  workflow_call:
  workflow_dispatch:

jobs:
  build_apk_dev:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 17

      - name: Install Flutter
        run: |
          java -version
          git submodule update --init --recursive
          .flutter/bin/flutter --version

      - name: Prepare Flutter Dependencies
        run: |
          .flutter/bin/flutter config --no-analytics
          .flutter/bin/flutter pub get

      - name: Get Android SDK Versions
        id: get-android-sdk-versions
        run: |
          find /usr/local/lib/android/sdk -name "source.properties" -exec cat {} + | grep Pkg.Revision | sort > sdk_versions.txt

      - name: Cache Android SDK
        id: cache-android-sdk
        uses: actions/cache@v2
        with:
          path: /usr/local/lib/android/sdk
          key: ${{ runner.os }}-android-sdk-${{ hashFiles('sdk_versions.txt') }}
          restore-keys: |
            ${{ runner.os }}-android-sdk

      - name: Set up Android SDK
        if: steps.cache-android-sdk.outputs.cache-hit == 'true'
        run: |
          echo "ANDROID_SDK_ROOT=/usr/local/lib/android/sdk" >> $GITHUB_ENV
          echo "ANDROID_HOME=/usr/local/lib/android/sdk" >> $GITHUB_ENV
          echo "PATH=${{ env.PATH }}:/usr/local/lib/android/sdk/emulator" >> $GITHUB_ENV
          echo "PATH=${{ env.PATH }}:/usr/local/lib/android/sdk/tools" >> $GITHUB_ENV
          echo "PATH=${{ env.PATH }}:/usr/local/lib/android/sdk/tools/bin" >> $GITHUB_ENV
          echo "PATH=${{ env.PATH }}:/usr/local/lib/android/sdk/platform-tools" >> $GITHUB_ENV

      - name: Build APK
        run: |
          .flutter/bin/flutter build apk --split-per-abi

      - name: Upload package
        uses: actions/upload-artifact@v4.3.1
        with:
          if-no-files-found: error
          name: dist_android
          path: |
            build/app/outputs/flutter-apk/*arm64*

      - name: Save Android SDK Cache
        if: always()
        uses: actions/cache@v2
        with:
          path: /usr/local/lib/android/sdk
          key: ${{ runner.os }}-android-sdk-${{ hashFiles('sdk_versions.txt') }}
