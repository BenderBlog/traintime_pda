## 通知系统消息国际化支持

由于项目使用的国际化方案基于 `flutter_i18n` 包，该包依赖于 `BuildContext`，
仅适用于 UI 层的国际化，而不适用于 Repository/Service 层。

为避免引入过多依赖或产生版本冲突，我们设计了一个轻量级的国际化工具类
用于通知系统的多语言支持。

### 原理

1. 使用 `build_runner` 将 YAML 配置文件中的键值对编译为各语言的静态 `Map` 对象
2. 维护一个服务类作为国际化信息的提供者
3. 其他服务类通过该服务类获取国际化文本

### 优势

- 📦 无额外依赖，避免版本冲突
- 🚀 编译期代码生成，运行时性能优势
- 🔄 与 `flutter_i18n` 保持一致的使用体验
- ⚡ 支持后台任务和通知系统等非 UI 场景

### 使用方法

#### 1. 添加或修改 YAML 文件

在 `assets/non_ui_i18n/` 目录下编辑对应语言的 YAML 文件：

```yaml
# zh_CN.yaml
course_reminder:
  title: "课前提醒：{name}"
  body: "{time} 分钟后开始上课"
  location: "地点：{location}"
  teacher: "教师：{teacher}"
```

#### 2. 运行 build_runner 生成代码

```bash
flutter pub run build_runner build --delete-conflicting-outputs
```

这将在 `lib/generated/` 目录下生成 `non_ui_i18n.g.dart` 文件。

#### 3. 在代码中使用
```dart
import 'package:watermeter/generated/non_ui_i18n.g.dart';

// 如果对应的翻译不存在，返回空字符串
String title = NonUII18n.translate("zh_CN", "course_reminder.title", translateParams: {"name": "Maths"});
```